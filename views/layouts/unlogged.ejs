<!DOCTYPE html>
<html lang="fr">
    <head>
        <title>hyperblender</title>
        <% include ../partials/global-metas %>
        <%- style %>
    </head>
    <body>
        <!-- Nav -->
        <div class="text-light bg-primary py-3">
            <div class="container">
                <h1>hyperblender</h1>
            </div>
        </div>
        <!-- Page content -->
        <div style="min-height: calc(100vh - 216px);">
            <div class="container">
                <div class="row">
                    <div id="alertZone" class="col-12 col-md-10 offset-md-1">
                        <% if(alertDanger.length > 0) { %>
                            <div class="alert alert-danger alert-dismissable fade show m-0 mb-2">
                                <%-alertDanger%>
                                <button type="button" class="close" data-dismiss="alert">
                                    <span>&times;</span>
                                </button>
                            </div>
                        <% } %>
                        <% if(alertSuccess.length > 0) { %>
                            <div class="alert alert-success alert-dismissable fade show m-0 mb-2">
                                <%-alertSuccess%>
                                <button type="button" class="close" data-dismiss="alert">
                                    <span>&times;</span>
                                </button>
                            </div>
                        <% } %>
                        <% if(alertInfo.length > 0) { %>
                            <div class="alert alert-info alert-dismissable fade show m-0 mb-2">
                                <%-alertInfo%>
                                <button type="button" class="close" data-dismiss="alert">
                                    <span>&times;</span>
                                </button>
                            </div>
                        <% } %>
                    </div>
                </div>
            </div>
            <!-- View -->
            <%-body%>
        </div>
        <footer class="py-5 bg-dark text-white">
            <% include ../partials/global-footer %>
        </footer>
        <% include ../partials/global-scripts %>
        <%- script %>
    </body>
</html>